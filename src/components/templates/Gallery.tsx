import React from 'react';

const Gallery = () => {
    return (
        <div>
            {/* - 갤러리 영역은 제공파일 내의 이미지들을 활용하여 제작한다. */}
            {/* - 갤러리 영역은 최소 3개 이상의 이미지를 사용하여 이미지 슬라이드를 구현하여야 한다. */}
            {/* - 갤러리 영역은 방향에 관계 없이 슬라이드된다. */}
            {/* - 사용자가 슬라이드 영역 위에 마우스 오버 시 슬라이드가 정지한다. */}
            {/* - 사용자가 슬라이드 영역 위에 마우스 오버 아웃 시 슬라이드가 다시 시작한다. */}
            갤러리
        </div>
    );
};

export default Gallery;