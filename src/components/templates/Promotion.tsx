import React from 'react';

const Promotion = () => {
    return (
        <div>
            {/* 프로모션 영역은 제공된 ‘Promotion.txt’ 파일의 내용을 참고하여 제작한다. */}
            {/* 프로모션 영역에는 최소 3개의 프로모션 아이템이 존재해야 한다. */}
            {/* 프로모션 아이템에는 아이콘과 프로모션 제목이 존재한다. */}
            {/* 프로모션 아이템에 마우스 오버 시 해당 프로모션의 정원 목록을 확인할 수 있다. */}
            프로모션
        </div>
    );
};

export default Promotion;